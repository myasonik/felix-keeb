# ─────────────────────────────────────────────────
# FelixKeeb shared keyboard config (left & right halves)
# ─────────────────────────────────────────────────

# Enable ZMK split-keyboard protocol
CONFIG_ZMK_SPLIT=y

# Battery reporting (nice!nano v2 has battery level sensing)
CONFIG_ZMK_BATTERY_REPORTING=y
